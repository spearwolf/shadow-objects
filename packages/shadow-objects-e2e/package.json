{
  "name": "shadow-objects-e2e",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "preview": "pnpm build && vite preview --host",
    "clean": "rimraf build dist playwright-report test-results",
    "setup:webkit": "node scripts/webkit-host-libs.mjs --install",
    "test": "node scripts/webkit-host-libs.mjs && rimraf test-results && playwright test",
    "test:ui": "node scripts/webkit-host-libs.mjs && rimraf test-results && playwright test --ui",
    "show-report": "playwright show-report",
    "update": "pnpm dlx npm-check --update"
  },
  "dependencies": {
    "@spearwolf/eventize": "catalog:",
    "@spearwolf/shadow-objects": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "rimraf": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:"
  }
}
