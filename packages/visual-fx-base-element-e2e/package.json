{
  "name": "visual-fx-base-element-e2e",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "pnpm exec vite",
    "build": "pnpm exec tsc && pnpm exec vite build",
    "preview": "pnpm exec vite preview",
    "clean": "pnpm exec rimraf build dist"
  },
  "dependencies": {
    "@spearwolf/visual-fx-base-element": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^4.4.5"
  }
}
